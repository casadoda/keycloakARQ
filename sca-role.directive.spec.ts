import { ScaRoleDirective } from './sca-role.directive';

describe('ScaRoleDirective', () => {
  it('should create an instance', () => {
    const directive = new ScaRoleDirective();
    expect(directive).toBeTruthy();
  });
});
